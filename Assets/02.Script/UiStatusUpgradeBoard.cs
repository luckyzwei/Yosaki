using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UiStatusUpgradeBoard : MonoBehaviour
{
    public void OnClickUpdateButton(string statusName)
    {
        //if (DatabaseManager.statusTable.tableDatas.TryGetValue(statusName, out var statusLevel) == false)
        //{
        //    Debug.LogError($"OnClickUpdateButton key {statusName} is not exist");
        //    return;
        //}


        //float currentGold = DatabaseManager.goodsTable.tableDatas[GoodsTable.Gold].Value;
        //float price = DatabaseManager.statusTable.GetStatusValue(statusName, statusLevel);


    }
}
